#include <stdio.h>

/**
 * main - dfd
 * @argc: df
 * @argv:sd
 * Return: df
 */

int main(int argc, char *argv[])
{
	if (argc != 3)
	{
		printf("Error");
		return (1);
	}

	printf("%d\n", (argv[1] * argv[2]));
	return (0);
}
