#include <stdio.h>

/**
 * main - df
 * @argc: sdkf
 * @argv: kj
 * Return: df
 */

int main(int argc, char *argv[])
{
	int count;

	for (count = 1; count < argc; count++)
	{
		printf("%s\n", argv[count]);
	}
	return (0);
}
