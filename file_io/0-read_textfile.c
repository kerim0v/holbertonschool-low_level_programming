#include "main.h"

/**
 * read_textfile - asfa
 * @filename:csad
 * letters: asda
 * Return: asfa
 */

ssize_t read_textfile(const char *filename, size_t letters)
{
	int fd = open(filename, O_RDONLY);
	if (fd == -1)
		return (0)

	if (filename == NULL)
	{
		return (0);
	}


	if (write == -1)
	{
		return (0);
	}

	close(fd);
}
